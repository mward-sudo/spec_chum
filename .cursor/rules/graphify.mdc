---
description: graphify knowledge graph — query before code explore
alwaysApply: true
---

# graphify

Graph at `graphify-out/`. **Before** Read/Grep/Glob/Bash **code explore**: run graphify first.

```bash
graphify query "<question>"
graphify path "<A>" "<B>"
graphify explain "<concept>"
```

Applies to you **and** every code-exploring subagent (put this in their prompt). Do not skip because files are “already known.”

**If graphify is unavailable:** consult the checked-in graph artifacts when useful, then continue with normal source exploration and note that the query was unavailable. Do not block code work solely on a missing CLI. Graph queries remain the preferred architecture entry point when available. For installation and PATH checks, see `AGENTS.md` → “graphify knowledge graph”.

**Skip a query when:** already oriented and editing specific lines, or `graphify-out/graph.json` is missing. Rule/doc-only Read/Write is fine after a quick query if helpful.

- Prefer `graphify-out/wiki/index.md` when present
- `GRAPH_REPORT.md` only for broad architecture when query/path/explain are insufficient

## Keep current

| Change | Command |
| --- | --- |
| Code | `./scripts/graphify_update.sh` (AST; commit `graphify-out/` when meaningful) |
| Docs/images | `./scripts/graphify_update.sh --full` (needs API key) |
| Optional | `./scripts/graphify_install_hooks.sh` (per-clone) |

CodeRabbit skips `graphify-out/**` via `.coderabbit.yaml` `path_filters`.

## After task done

Skim `GRAPH_REPORT.md` Suggested Questions → **do now** / **defer** (must file/link issue) / **ignore** (short rationale). Don’t derail the active branch for speculative refactors.
