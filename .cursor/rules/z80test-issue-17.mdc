---
description: Issue #17 DONE — keep z80doc green; z80full opt-in/ignored
alwaysApply: true
---

# Issue #17 — z80test integration (DONE)

Track: https://github.com/mward-sudo/spec_chum/issues/17 (closed)

Real Patrik Rak [z80test](https://github.com/raxoft/z80test) TAP runner lives in `crates/machine/src/z80test.rs` under `--features slow-tests`. **Do not reopen #17** or treat the harness as a stub.

## Current expectations

1. **`z80doc`** — checked-in fixture; keep CI / opt-in slow-tests green:
   `cargo test -p machine --features slow-tests --release z80doc_all_tests_passed`
2. **`z80full`** — opt-in / `#[ignore]` for day-to-day and default CI; fetch with
   `./scripts/fetch_z80test.sh`, then run with `--ignored`. Not required to keep
   #17 closed. **Required before a `vX.Y.Z` release** (via `./scripts/run_slow_tests.sh`
   or `SYSTEM_TESTS_Z80FULL=1 ./scripts/run_system_tests.sh`) — see `docs/RELEASE.md`.

## Agent guardrails

- Prefer CI opt-in / `slow-tests` for full suites; keep default `cargo test` fast.
- Fixtures: `tests/fixtures/z80test/` (see that README).
- Open accuracy follow-ups are elsewhere (e.g. [#33](https://github.com/mward-sudo/spec_chum/issues/33) AY, [#34](https://github.com/mward-sudo/spec_chum/issues/34) border/beam).
