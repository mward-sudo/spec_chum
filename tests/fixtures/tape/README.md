# Tape fixtures

Freely redistributable TAP images for automated tests.

| File | Purpose |
| --- | --- |
| `minimal_code.tap` | Hand-crafted CODE header + 6-byte data block (`LD HL,4000 / LD (HL),42 / RET` at `0x8000`) |

Do not add commercial game TAPs.
